-- Initial Data for Remaining Hobby Categories
-- These tables will start empty and be populated manually through the application

-- Wanderlog (Travel) - Empty to start
-- Fields: city, country, area, attractions(tags), highlight, date(mm/yy), star rating, photos link, sample images (multi-image gallery), cover image, comment
-- INSERT INTO wanderlog (...) VALUES (...);

-- Scribbles (Doodles & Sketches) - Empty to start  
-- Fields: name, image(prominent), genre, category, date, description
-- INSERT INTO scribbles (...) VALUES (...);

-- Shutter Tales (Photography) - Empty to start
-- Fields: name, image(prominent), location, genre, category, date, description  
-- INSERT INTO shutter_tales (...) VALUES (...);

-- Spot Light (Personal Portfolio) - Empty to start
-- Fields: image(prominent), location, genre, category, date, description
-- INSERT INTO spot_light (...) VALUES (...);

-- Note: These hobby categories will be populated through the application interface
-- as users upload images and add their creative works and travel experiences. 